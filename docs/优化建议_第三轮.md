# 优化建议（第三轮·进度总览）

日期：2026-05-29

## 已完成
N+1 优化 / Blueprint 拆分 / 账号统一 / 列表分页 / 404·500 错误页 /
脚本整理(tools) / 冒烟测试 / 配置清理(强制 DATABASE_URL) /
导航高亮修复 / 仪表盘卡片可点击 / 积分抵现闭环 / 删除二次确认(已存在)

## 还可优化

### A. 答辩展示向（性价比最高）
1. 统计页 `/analytics` 图表化：8 个报表多为表格，挑 2~3 个做柱状/折线（纯 CSS，
   复用仪表盘现有样式）。观感提升最大。
2. README + 数据库 ER 图 + 截图。卖点是多表关系，ER 图最直观。
3. 报表导出 CSV。

### B. 功能补全
4. 菜单 / 库存加搜索筛选（与会员、订单页体验一致）。
5. 操作日志表（谁、何时、改了什么）——又一组表关系，契合主题。

### C. 工程健壮性
6. 数据库索引：当前 0 个自定义索引；给 orders.order_time 及外键列加索引
   （需配套迁移脚本），提速且可写进报告。
7. 下单输入校验：购物车为空仍可提交 ¥0 空单；数量非数字会报错。后端补校验。
8. 密码哈希存储：登录仍明文比对，Werkzeug 已在依赖里，改哈希（需迁移脚本）。
9. 移动端适配：导航栏窄屏拥挤，加换行/汉堡菜单。

## 推荐优先级
A1（统计页图表）最值得先做（答辩加分 + 可复用现有样式），
其次 B5（操作日志，契合“多表关系”主题）。
