# 员工账号调整：删除重复许跃骞、xyq 补手机号

完成日期：2026-05-29

## 背景

早期演示数据里有两个许跃骞账号：`xuyueqian`（staff_id 4）和 `xyq`（staff_id 5）。
你本地数据库其实已经手动删过 `xuyueqian` 并给 `xyq` 加了手机号，
但**种子脚本没同步**，导致搭档杨帆重新跑会又冒出多余账号。本次把脚本对齐。

## 现在的员工（你本地已是此状态）

| staff_id | 用户名 | 姓名 | 角色 | 手机号 |
|---|---|---|---|---|
| 1 | admin | 杨帆 | ADMIN | 13800000001 |
| 2 | barista | 李明 | BARISTA | 13800000002 |
| 3 | cashier | 王磊 | CASHIER | 13800000003 |
| 5 | xyq | 许跃骞 | ADMIN | 13812340567 |

## 改了哪些文件

1. **`src/bootstrap.py`** — 演示员工列表去掉重复的 `xuyueqian`，全新安装不再创建它。
2. **`src/seed_admin.py`** — 幂等给 `xyq` 加手机号 `13812340567`（不存在则新建带手机号，已存在但没号则补，已有则跳过）。
3. **`src/seed_cleanup_staff.py`**（新增）— 幂等删除已存在的 `xuyueqian`；若它已下过订单则不删、改为停用，避免外键报错。
4. **`CLAUDE.md`** — 账号表同步更新。

## 搭档杨帆需要做什么

`git pull` 后，进 `src` 目录依次运行一次：

```
.\venv\Scripts\python.exe seed_cleanup_staff.py
.\venv\Scripts\python.exe seed_admin.py
```

## 验证

- 三个脚本在本地重复执行均幂等、无报错。
- 用 `xyq/070203` 登录，员工页正常返回 200。

提交：`68bb41f`，已推送到 GitHub。
